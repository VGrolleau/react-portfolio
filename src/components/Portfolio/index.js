import './index.scss';
import Loader from 'react-loaders';

const Portfolio = () => {
    return (
        <>
            <div className='container portfolio-page'></div>
            <Loader type='pacman' />
        </>
    )
};

export default Portfolio;